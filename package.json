{
  "name": "fidelity-portfolio-tracker-scripts",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "concurrently -k -n api,web \"npm run start:api\" \"npm run start:web\"",
    "start:api": "set FLASK_ENV=development&& set PYTHONIOENCODING=utf-8&& venv\\Scripts\\python.exe -m flask --app src.server run --debug --host 0.0.0.0 --port 8000",
    "start:web": "npm --prefix client run dev -- --host 0.0.0.0"
  }
}
